## HTTPS란?

HTTP의 전송 계층에 **데이터를 암호화하고 보안을 강화**한 통신 규약입니다.

→ 이유 : **데이터를 암호화하여 전송의 안전성을 보장**하기 위해서 사용한다.

알고있어야하는 짜잘한 개념

**비밀키와 공개키**는 서로 짝(pair)이다!

→ 공개키와 비밀키는 동시에 생성됨. (서버는 공개키와 비밀키 둘 다 갖고있음. )

**서버의 인증서 발급 과정**

1. 왕은 **비밀키 공개키 둘다 생성 (한 쌍의 키 생성)**
    1. 비밀키 = 개인키 서버만 가지고 있음
    2. 즉, 본인만 알고 있음.

1.  왕은 본인의 민증이 필요함. → 사무소에 민증 요청
- 신원 정보
- **개인키 (손가락 지문 비슷)**
- **공개키**

1. 동사무소에서 확인 결과가 나오면 디지털 인증서(민증)를 보내줌
    1. 이때, 동사무소도 개인키(비밀키)로 서명해줌

1. 신난 왕은 본인 성에 **인증서 설치**
2. 앞으로 왕의 성에 방문할 때는 장군에게 본인의 성임을 인증서를 통해 인증합니다.
    1. 장군 : 인증서의 왕의 **공개키와 동사무소의 공개키로 인증서의 진위여부**를 확인합니다.

---

**장군 → 왕 요청 과정**

장군은 전투를 끝내고 성으로 들어가려고합니다.

1. 장군도 CA(동사무소)로 가서 본인의 공개키로 인증서를 발급받음.
2. 인증서 발급 후 서버에게 요청 → 서버가 본인의 CA 인증서를 줍니다.
3. CA 인증서(CA 공개키 서버 공개키)
4. **장군이 데이터를 보내고 싶을때, 클라이언트 비밀키(본인이라는 인증) + 서버 공개키로 암호화**
5. **서버가 받고, 서버의 비밀키로 복호화**

→ 하지만 대부분의 웹사이트에서는 클라이언트의 키 쌍을 생성하지 않는다고함

---

- **TLS 인증서**
    - 개념 : 공개키, 도메인 정보, CA 서명등이 포함된 공개문서
    - 브라우저가 서버의 정체를 검증함.
    - **서버가 진짜임을 인증하는 디지털 신분증임**
    - 예시 : 네이버

  ![image.png](attachment:038d7a2d-6b77-4e0b-bec9-d92821acf6dc:image.png)


**인승서 확인 방법 :** URL 옆에 있는 사이트 정보보기 아이콘을 클릭하시면 “이 연결은 안전합니다”라는 버튼을 통해 확인할 수 있습니다

TLS 인증서가 없으면 안전 보증이 안됨.

- 진짜 사이트가 아닐경우, 보안이 위반됨

2018년, TLS 1.3이 등장하였습니다.

TLS 1.2보다 훨씬 과정이 단순화되었으며, 더 빠르고 보안이 좋아졌다고 합니다.

TLS 1.2에서 TLS 1.3으로 어떻게 변화하였는지 알아보려합니다.

![image.png](attachment:55d2825f-2e84-455b-ad31-90aea101d2da:84a04f74-c22c-43c5-a955-07cedbb87732.png)

### TLS 1.2 Handshake 과정 | 서버과 클라의 대칭키 교환 방식

RSA는 완전히 사라지지 않았지만, TLS 1.3에서는 RSA 기반 키 교환 방식이 제거되고 ECDHE 기반의 임시 키 교환이 기본으로 사용됩니다. RSA는 주로 인증서의 공개키/서명 용도로 남아있습니다.

### 복잡해서 눈물 나올것같을 땐

- **클라이언트** → 접속을 시도하는 쪽 **(**크롬, 사파리 엣지 등)

사이트 인증 과정

1. 클라이언트 접속
    1. **`client_random**` 🟥생성해서 서버로 줌 서버 ㅎㅇㅎㅇ
2. 서버가 인증서 줌
    1. 오 ㅎㅇㅎㅇ 내 랜덤값 여기 **`server_random` 🟨 줄께**
    2.  나는 진짜 **네이버야 이건 내 공식 인증서! TLS 인증서(서버 공개키)와 서버랜덤값을 건네며**
3. 브라우저가 확인함
    1. 오 진짜네 CA가 보증했네
4. 브라우저(클라이언트)가 **비밀 하**나 만듦🟩 `pre-master secret`
    1. **랜덤한 비밀값 생성 - 무작위로 아주 큰 숫자를 만듬**
    2. 이건 우리끼리만 쓸 암호키야
5. 그 비밀을 서버에게만 읽을 수 있게 암호화해서 보냄
    1. **클라이언트가 비밀 값[클라이언트 - 크롬]을 공개키[서버 - TLS 인증서로 받은 서버의 공개키]로 암호화**해서 **서버로** 보내니까 **서버**만 해독 가능
    2. 서버는 **비밀 값[클라이언트 - 크롬]을 본인만의** **개인키로 복호화해서 비밀값** 🟩`pre-master secret` 획득
6. 서버와 클라가 **동일한 대칭키를 계산함.**
    1. **그렇게 서버와 클라가 3가지 값을 모두 알게됨.**
        1. 🟩pre-master secret(비밀값)
        2. 🟥client_random(브라우저가 보낸 랜덤값 [클라 공개키])
        3. **🟨**server_random (서버가 보낸 랜덤값[복호화해서 얻은 비밀값])
    2. 위의 값은 **서버와 클라가 모두 알고 있음.**
    3. **세션 키(대칭)** = **if(allNotNull) PRF(pre-master, client_random, server_random)**
        1. 모두 3가지 값을 알경우 **PRF 대칭키 계산**
        2. **대칭키는 각자 따로 계산함**
            1. 대칭키를 **한 쪽이 만들고 보내면 → 도청 위험**
7. 서로 같은 대칭키(세션키)를 계산했는지 확인함.
    - 확인하는 방법 :
        1. 클라가 먼저 대칭키(세션키)로 Finished 메시지를 암호화해서 서버로 보냄
        2. 서버가 메시지 받고 자기 대칭키(세션키)로 복호화
            1. Finished 메시지가 정상적으로 나옴 → 오 너 나랑 똑같구나

           → 그럼 대칭키로 모든 데이터 암호화

8. 서버랑 나랑 똑같은 비밀키를 공유하게 됨.
    1. 그 키로 모든 대화는 암호화됨.
- 대칭키의 장점 : 빠르게 서로 소통할 수 있음.

![image.png](attachment:fede67e7-ff61-4cbb-a9f4-8fbdac78c241:image.png)

즉, **브라우저는 암호화(공개키가 있어서) 하고 → 서버가 복호화(개인키가 있어서 )**

- **공개키 :** 그냥 암호화용 열쇠임. **누구나 만들수있음 즉, 누구나 잠글수있음.** → 열쇠를 누구나 만들수있는것처럼
- **개인키 :** 서버만 가지고 있어서 **서버만 열수있음. 복호화를 위한 키**
- 암호화/ 복호화 : 공개키로 암호화걸고 개인키로 복호화함. [공개키 암호화의 기본원리 ]
- **대칭키 : 같은 키를 이용해서 암호화/복호화**하는 방식에서 사용되는 키
    - 외부에서 해당 키를 알게 되면, 안전에 위험이 있기 때문에 각자 해당 키를 안전하게 가지고 있어야함.
- **비대칭 키:** 서로 다른 **두개의 키를 이용해서 암호화/복호화하는 방식**에서 사용되는 키
    - 대칭키에 비해 과정이 복잡하여 속도가 느림.
    - 처음에만 비대칭키로 데이터를 교환하고 후에는 대칭키르르 사용함.

- **그걸 진짜 열쇠로 만들어주기 위해 TLS인증서에 포함시켜서 CA가 보증**

즉. 브라우저랑 서버가 둘만 아는 비밀키를 만들어서 그걸로 안전하게 수다 떨기 시작하는게 TLS Handshake

- 하지만 최신 HTTPS **TLS 1.3부터는** RSA를 사용하는 것이 아니라 ECDH또는 디피헤르만 방식 채택
    - 각자 임시 키쌍을 만들어서 교환후 세션키 생성 시 임시 키 쌍을 제거한다고 합니다.

[](https://blog.naver.com/ucert/221234082080)

[TLS handshake 과정](https://jeongchul.tistory.com/752)

[RFC 5246: The Transport Layer Security (TLS) Protocol Version 1.2](https://datatracker.ietf.org/doc/html/rfc5246?utm_source=chatgpt.com)

---

쉬운 이해를 위해 단계 위주로 작성하였습니다. 때문에 더욱 자세한 정보 기입은 배제하였으므로 자세한 사항은 레퍼런스를 확인해주시면 감사하겠습니다.

### TLS 1.3

TLS 1.3의 이점

- 핸드셰이크 지연 시간 감소
- 핸드셰이크를 더 많이 암호화
- 프로토콜 간 공격에 대한 회복력 향상

![image.png](attachment:3b2ca93d-9973-44af-a8c2-11829fd34aa6:image.png)

RSA 방식 (요약)

1. ClientHello (client_random)
2. ServerHello + Certificate(서버 공개키) + server_random
3. Client: `pre-master` 생성 → **서버 공개키로 암호화** → 전송
4. Server: 비밀키로 복호화 → 둘이 `pre-master+randoms`로 세션키 파생
5. Finished 교환 → 암호화된 통신

ECDHE 방식 (요약)

1. ClientHello (client_random + client_key_share(임시 공개키))
2. ServerHello (server_random + server_key_share(임시 공개키)) + Certificate(서명으로 인증)
3. **받은 공개키와 각자의 개인키 공유비밀 계산 (전송된 건 공개키뿐)** → 세션키 파생
4. Finished 교환 → 암호화된 통신

핵심: RSA는 **비밀을 전송**(암호화해서 보냄). ECDHE는 **비밀을 전송하지 않음**(각자 계산).

예)

- A: 개인키 `a` 생성 → 공개키 `A` 계산 → `A`를 B에 보냄.
- B: 개인키 `b` 생성 → 공개키 `B` 계산 → `B`를 A에 보냄.
- A: 받은 `B`와 자기 `a`로 `shared = a * B` 계산.
- B: 받은 `A`와 자기 `b`로 `shared = b * A` 계산.

  → 수학적으로 `a·B = a·(b·G) = b·(a·G) = b·A`. 둘 다 같은 값.

  **`shared`를 HKDF(또는 RFC8446 규정의 키 스케줄)에 넣어 세션키들(암호화키, IV, MAC 키 등) 을 만든다.**


[A Detailed Look at RFC 8446 (a.k.a. TLS 1.3)](https://blog.cloudflare.com/rfc-8446-aka-tls-1-3/?_gl=1*1ex93n2*_gcl_au*MTA2MTY2MjcyOS4xNzU4NzgxNzU0*_ga*ZjU3Yzk4ZDEtNGM0Ni00MDc5LTlhN2UtOGM0OGU4ZjhjMDAz*_ga_SQCRB0TXZW*czE3NTg3ODE3NTMkbzEkZzEkdDE3NTg3ODIzMDkkajYwJGwwJGgw/)

[TLS 1.3를 사용하는 이유는? | SSL과 TLS의 취약점 | Cloudflare](https://www.cloudflare.com/ko-kr/learning/ssl/why-use-tls-1.3/)

---

### TLS 1.2→1.3 RTT의 변화

RTT(Round-Trip Time ) : 네트워크 요청이 시작점에서 목적지에 도달했다가 다시 시작점으로 돌아오는 왕복에 걸린 시간

> **TLS 1.2 (typical ECDHE)**
>
- 클라 → 서버: ClientHello
- 서버 → 클라: ServerHello + Certificate + ... ← **1 RTT 도달**
- 클라 → 서버: (키 교환/Finished) ← 이후 암호화된 애플리케이션 데이터 가능
- 서버 → 클라: Finished + 암호화된 응답 ← **총 약 2 RTT로 완전 완료**.

> **TLS 1.3 (기본 full handshake)**
>
1. 클라 → 서버: ClientHello (client_key_share 포함)
2. 서버 → 클라: ServerHello (server_key_share 포함) + 인증/Finished
3. 클라 → 서버: Finished → 이 시점에 양쪽 모두 **세션키로 암호화 통신 시작 가능.**

   → **핸드셰이크 왕복이 줄어 들어 실제로는 1 RTT 안팎으로 연결 준비 완료**.


> **TLS 1.3의  0-RTT**
>

**TLS 1.3 (재접속, 0-RTT 사용 시)**

- **0-RTT =** 재접속 시, 클라이언트가 **“이전에 받은 세션 티켓”(PSK : Pre-Shared Key 사전 공유 키 )**을 사용해 서버에 요청(애플리케이션 데이터)을 **바로 같이 보내는 것**.

→ 전송이 빠르지만 공격에 취약하기 때문에 민감한 정보 요청에는 권장하지 않는다. 예) 결제 및 송

[TLS 1.2 vs. 1.3—Handshake, Performance, and Other Improvements](https://www.catchpoint.com/http2-vs-http3/tls1-2-vs-1-3?utm_source=chatgpt.com)

---

## 회고

가뜩이나 이해력이 부족한데, 이상한 개념에 꽂혀서 여기까지 들어왔네요… 제가 TLS 이해할 때 너무 어려워했어서 이 글을 보시는 동료께 이해하는데에 도움이 되었으면 좋겠습니다.

혹여나 잘못된 정보를 작성했을까봐 두려워서 올리지 않을려했는데 너무 열심히 조사하고 공부해서 올리게 되엇읍니다…