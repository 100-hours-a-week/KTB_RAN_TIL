# 코딩테스트

## 목차

- [코딩테스트](#코딩테스트)
  - [목차](#목차)
  - [시뮬레이션](#시뮬레이션)
    - [완전탐색](#완전탐색)
      - [for](#for)
        - [- 구간의 동전 수 세기 : 완전탐색 격자](#--구간의-동전-수-세기--완전탐색-격자)
        - [- 격자 안 밀고 당기기 : 시뮬레이션 / 배열 회전 / 밀기](#--격자-안-밀고-당기기--시뮬레이션--배열-회전--밀기)
        - [격자 안에서 터지고 떨어지는 경우 : 시뮬레이션 / 배열 재구성 / 중력 처리](#격자-안에서-터지고-떨어지는-경우--시뮬레이션--배열-재구성--중력-처리)
          - [temp 사용 : 현재 배열에서 삭제하고, 남은 배열을 다시 현재 배열로 바꾼다](#temp-사용--현재-배열에서-삭제하고-남은-배열을-다시-현재-배열로-바꾼다)
        - [격자 안에서 단일 객체 이동](#격자-안에서-단일-객체-이동)
      - [백트래킹](#백트래킹)

## 시뮬레이션

### 완전탐색

#### for

##### - 구간의 동전 수 세기 : 완전탐색 격자

##### - 격자 안 밀고 당기기 : 시뮬레이션 / 배열 회전 / 밀기

    temp 사용

```jsx
def move():
    temp = u[-1]  # 윗줄 마지막 값 저장

    # 윗줄 오른쪽으로 밀기
    for i in range(n - 1, 0, -1):
        u[i] = u[i - 1]

    # 아랫줄 왼쪽으로 밀기
    for i in range(n - 1):
        d[i] = d[i + 1]

    # 연결되는 부분 처리
    u[0] = d[0]   # ❌ 이러면 안 됨
```

##### 격자 안에서 터지고 떨어지는 경우 : 시뮬레이션 / 배열 재구성 / 중력 처리

###### temp 사용 : 현재 배열에서 삭제하고, 남은 배열을 다시 현재 배열로 바꾼다

```jsx
for s, e in deleted:
    new_blocks = []

    for idx in range(len(blocks)):
        if s - 1 <= idx <= e - 1:
            continue
        new_blocks.append(blocks[idx])

    blocks = new_blocks
```

##### 격자 안에서 단일 객체 이동

시뮬레이션 / 격자 이동 / 단일 객체 이동

> 격자 문제 템플릿
>
> ```jsx
> # 상 하 좌 우
> dr = [-1, 1, 0, 0]
> dc = [0, 0, -1, 1]
>
> def in_range(r, c):
>     return 0 <= r < n and 0 <= c < n
>
> curR, curC = startR, startC
>
> for i in range(4):
>     nr = curR + dr[i]
>     nc = curC + dc[i]
>
>     if in_range(nr, nc):
>         ...
> ```

#### 백트래킹

- **arr[행][열] = arr[y][x]**
