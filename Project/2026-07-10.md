# 코딩테스트

## DP

1. **topDown(재귀)** : **메모이제이션**은 높은 수에서 낮은 수로 내려가기 때문에, f(n-1)+f(n-2)등을 사용하므로
2. **bottomUp(반복문) : tabulation**은 \*\*\*\*아래에서 채워 나가기 때문에 for i in 1~ k : d[i] =

**시간복잡도는?**

비슷하지만 **탑다운은** 재귀적으로 여러번 실행되어야 하기 때문에 **함수처리 시간이 약간 존재**

그래서 바텀업이 더 빠름

**언제 쓰나요?**

어떤 문제를 사용해도 괜찮지만, 특정 문제는 한 문제로 더 쉽기 때문에 둘다 할줄알아야됨.

## DFS/BFS

- DFS : 재귀호출 사용하여 깊은 곳까지 들어감 (재귀 특징을 이용한 탐색)
  - 헷갈릴 것 없다! DP, 재귀와 비슷한 항목
- BFS : 큐를 사용하여 인접한 노드(친구)를 먼저 방문한다.
